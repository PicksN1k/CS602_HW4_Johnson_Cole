<h2>Course ID Query</h2>{{query}}

<ul>
{{#each courses}}
    <li>
        <a href="/describe/{{_id}}">{{_id}}</a>

        {{courseName}}

        (<a href="/coordinator/{{coordinator._id}}">
            Contact Prof. {{coordinator.lastName}}
        </a>)
    </li>
{{/each}}
</ul>

<p><a href="/">Home</a></p>